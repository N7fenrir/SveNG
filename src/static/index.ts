const TOP_LEFT = {x: 0, y: 0};  // holds top left of canvas in world coords.
const GRID_LIMIT = 64;  // max grid lines for static grid
const GRID_SIZE = 128;  // grid size in screen pixels for adaptive and world pixels for static
const SCALE_RATE = 1.02; // Closer to 1 slower rate of change
const GRID_SCREEN_SIZE = 128
const GRID_STROKE_COLOR = "#000";




export {TOP_LEFT, GRID_SIZE, GRID_LIMIT, SCALE_RATE, GRID_SCREEN_SIZE, GRID_STROKE_COLOR}